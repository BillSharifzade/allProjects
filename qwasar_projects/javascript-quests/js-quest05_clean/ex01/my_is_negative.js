function my_is_negative(n) {
    if (n < 0) {
      return 0;
    } else {
      return 1;
    }
  }
  
  // Test cases (these should be commented out for submission)
  // console.log(my_is_negative(-1));  // Should print 0
  // console.log(my_is_negative(1));   // Should print 1
  // console.log(my_is_negative(0));   // Should print 1
  // vot koroche
  